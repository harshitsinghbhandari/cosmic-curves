{
  "name": "cosmoscurves",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "./start.sh",
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "backend": "cd backend && python -m uvicorn main:app --reload",
    "landing:dev": "cd landing && npm run dev",
    "landing:build": "cd landing && npm run build",
    "landing:preview": "cd landing && npm run preview"
  }
}
