{
  "name": "cosmoscurves-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "laptop-pwa",
    "phone-pwa"
  ],
  "scripts": {
    "build": "npm run build -w laptop-pwa && npm run build -w phone-pwa && rm -rf public && mkdir -p public && cp -rv laptop-pwa/dist public/laptop && cp -rv phone-pwa/dist public/phone && cp index.html public/"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
